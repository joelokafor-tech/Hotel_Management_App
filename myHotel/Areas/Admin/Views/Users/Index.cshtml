@* @{
    ViewData["Title"] = "Users";
}

<div class="container mt-4">
    <h2>Welcome to the Admin Dashboard</h2>
    <p class="text-muted">This is the Users management page (placeholder).</p>
</div> *@

@* @model IEnumerable<myHotel.Areas.SecureAccess.Models.ApplicationUser> 

@{
    ViewData["Title"] = "Manage Users";
}

<h2>Users</h2>

 <table class="table table-striped">
    <thead>
        <tr>
            <th>Email</th>
            <th>Full Name</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var user in Model)
        {
            <tr>
                <td>@user.Email</td>
                <td>@user.FullName</td>
            </tr>
        }
    </tbody>
</table> *@


@* <table class="table table-striped">
    <thead>
        <tr>
            <th>Email</th>
            <th>Full Name</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var user in Model)
        {
            <tr>
                <td>@user.Email</td>
                <td>@user.FullName</td>
                <td>
                    <a asp-action="ManageRoles" asp-route-userId="@user.Id" class="btn btn-sm btn-info">Manage Roles</a>
                </td>
            </tr>
        }
    </tbody>
</table> *@


@model IEnumerable<myHotel.Areas.Admin.ViewModels.UserWithRolesViewModel>

@{
    ViewData["Title"] = "Users";
}

<h2 class="mb-4">Users</h2>

@if (TempData["SuccessMessage"] != null)
{
    <div id="alertMessage" class="alert alert-success">@TempData["SuccessMessage"]</div>
}

@if (TempData["ErrorMessage"] != null)
{
    <div id="alertMessage" class="alert alert-danger">@TempData["ErrorMessage"]</div>
}

<table class="table table-bordered table-striped">
    <thead class="table-dark">
        <tr>
            <th>Full Name</th>
            <th>Email</th>
            <th>Roles</th>
            <th>Actions</th>
            @* <th>ActionsRole</th> *@
        </tr>
    </thead>
    <tbody>
        @foreach (var user in Model)
        {
            <tr>
                <td>@user.FullName</td>
                <td>@user.Email</td>
                <td>
                    @if (user.Roles.Any())
                    {
                        @string.Join(", ", user.Roles)
                    }
                    else
                    {
                        <span class="text-muted">No roles</span>
                    }
                </td>
                @* <td>
                    <a asp-action="ManageRoles" asp-route-userId="@user.Id" class="btn btn-sm btn-info">Manage Roles</a>
                </td> *@
                <td>
                    <a asp-action="ManageRoles" asp-route-id="@user.Id" class="btn btn-sm btn-primary">Assign Role</a>
                    <a asp-action="Edit" asp-route-id="@user.Id" class="btn btn-sm btn-warning">Edit</a>
                    <a asp-action="Delete" asp-route-id="@user.Id" class="btn btn-sm btn-danger">Delete</a>
                </td>

            </tr>
        }
    </tbody>
</table>


@section Scripts {
    <script>
        setTimeout(function () {
            var alertBox = document.getElementById("alertMessage");
            if (alertBox) {
                alertBox.style.transition = "opacity 0.5s ease";
                alertBox.style.opacity = "0";
                setTimeout(() => alertBox.remove(), 500);
            }
        }, 3000);
    </script>
}